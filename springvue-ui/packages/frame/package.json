{
  "name": "frame",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite serve --mode development",
    "build": "eslint --fix  --ext .js,.vue,.ts ./src/ && vue-tsc && vite build --mode production",
    "preview": "vite preview"
  },
  "dependencies": {
    "common": "^1.0.0"
  }
}
