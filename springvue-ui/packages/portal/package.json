{
  "name": "portal",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "eslint --fix  --ext .js,.vue,.ts ./src/ && vue-tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "qiankun": "^2.8.3"
  }
}
